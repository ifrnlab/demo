# Demo

Repositório de demonstração.

## Roteiro

1. Criar e abrir a pasta `demo`
2. Abrir o Git Bash na pasta
3. Execute os seguintes comandos:
   - `pwd`
   - `ls`
   - `git init`
   - `ls`
   - `ls -a`
   - `ls -Fa1`
   - `ls -FA1`

4. Vamos criar arquivos. Execute:

   - `touch index.html`